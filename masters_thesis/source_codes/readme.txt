The local and nonlocal damage models are implemented for the research.

This directory contains both models.
You can compile those by running compile.sh
Eigen (Linear algebra library) should be installed and it's path should be included.

To run simulations with RVES, RVE_geometry_generator is provided.
